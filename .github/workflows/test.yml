name: Test pull Request changes

on: 
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]
jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello from pull request